#include "main.h"

/**
 * mul - multiplication of two integers
 * @m: one of the integer
 * @n: another integer
 * Return: the product of m and n
 */

int mul(int m, int n)
{
	int p;

	p = m * n;

	return (p);
}
