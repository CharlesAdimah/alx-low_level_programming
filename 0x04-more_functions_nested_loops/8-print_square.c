#include "main.h"

/**
 * print_square - prints a square, followed by a new line
 * @z: size of the square
 */

void print_square(int size)
{
	if (z <= 0)
	{
		_putchar('\n');
	}
	else
	{
		int i, j;

		for (i = 0; i < z; i++)
		{
			for (j = 0; j < z; j++)
			{
				_putchar('#');
			}

			_putchar('\n');
		}
	}
}
