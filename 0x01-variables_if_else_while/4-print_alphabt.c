#include <stdio.h>

/**
 * main - print lowercase alphabet except q and e
 * Return: Always 0 (When successful)
 */
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}

