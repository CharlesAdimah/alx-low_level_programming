#include <stdio.h>

/**
 * main - prints single base 10 digits 
 * starting with 0
 * Return: Always 0 (When successful)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
